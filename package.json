{
  "name": "imkon-sugurta",
  "version": "1.0.0",
  "private": "true",
  "scripts": {
    "sass": "sass --watch dist/scss:dist/css --style compressed",
    "sass-build": "sass dist/scss:dist/css --style compressed",
    "prefix": "postcss dist/css/global.css --use autoprefixer -d dist/css/",
    "build": "yarn sass-build && yarn prefix"
  },
  "keywords": [],

  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "postcss": "^8.3.5",
    "postcss-cli": "^8.3.1",
    "sass": "1.32.13"
  },
  "author": "https://github.com/Sardor01",
  "license": "ISC",
  "browserslist": "last 4 versions"
}